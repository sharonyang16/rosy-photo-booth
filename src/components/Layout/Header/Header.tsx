import Typography from "@/components/base/Typography";

const Header = () => {
  return (
    <header className="py-8">
      <Typography variant="title">Rosy Photo Booth</Typography>
    </header>
  );
};

export default Header;
